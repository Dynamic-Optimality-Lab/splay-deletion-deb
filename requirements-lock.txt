# requirements-lock.txt — WP-1 exact runtime dependencies: stdlib only.
# python: 3.13.7 (requires-python >= 3.12 satisfied; spec recommends 3.12.x)
# third-party packages: NONE for WP-1 gates (json, hashlib, math, itertools,
# subprocess, sys, os from the standard library only).
# zstandard is installed in this environment and used for .zst transport
# files; it carries no scientific content (compression outside canonical
# hashing: logical-stream SHA-256 recorded separately).
# Discovery/analysis backends introduced in WP-2 (pinned exact versions):
# scipy==1.16.3 — HiGHS LP backend for DISCOVERY PROPOSALS ONLY
#   (scipy.optimize.linprog, method='highs'). Float output is quarantined
#   (authoritative=false) and can never seal an exact result (threat T6).
# jsonschema==4.25.1 — validates sealed artifacts against schemas/*.schema.json.
# zstandard — .zst transport (no scientific content).
# WP-1 gates remain stdlib-only; the above are used only where stated.
