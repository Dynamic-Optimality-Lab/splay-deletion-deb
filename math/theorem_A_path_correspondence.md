# Theorem A — path correspondence (T0-01..T0-03)

Statements: diagonal-rooted KEEP/DELETE paths biject with `(T, X, Y ⪯ X)`;
path `(a, y)` sums equal `(Splay(X,T), Splay(Y,T))`; hence
`b_n* = sup` over nonempty diagonal-rooted path ratios.
Full proofs: FORMAL_NOTES.md §§T0-01..T0-03. Mechanically backed by gate
tests M04 (exhaustive `n≤5` triple agreement on all `(T, x)` transitions).
