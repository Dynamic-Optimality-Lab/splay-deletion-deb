# Math definitions (frozen glossary; proofs in FORMAL_NOTES.md)

`[n]`, `T_n`, `C_n`, `d_T`, `c = depth+1`, `S_x`, ZIG/LL/RR/LR/RL, `Splay(X,T)`,
`Y ⪯ X`, `(A,B)`, `Δ_n`, KEEP/DELETE with `(a, y)`, edge `(source, mode, key)`,
edge order `K(1..n),D(1..n)`, `X(P)`, `Y(P)`, `R_n` + forward closure,
`w_b`, `ℓ_b`, `L_{p,q}`, validity, `b_n*`, `1 ≤ b_n* ≤ n`,
TRANSIENT/CYCLIC/MIXED, `U_b`, `V_b`, `G_b`, FORCED, `F_{n,b}`,
`r_H`, FPATH/FCYCLE/FGAP/VBELLMAN/UBELLMAN → FORCED_DELTA,
`ΔH(e) = ℓ_b(e)`, criticality subtype vs top-level status,
shape grammar, `tree_id`, `pair_id`, `reachable_index`,
OG-1..OG-3, UH-0..UH-9, BH01–BH05, `reserved_family_holdout`.

All meanings identical to IMPLEMENTATION_SPEC.md §§1–16.
